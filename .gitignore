node_modules/
.next/
.env*.local
.DS_Store
*.log
.vercel
.superpowers/
.sanity/
dist/
/tools/
.claude/
content-drafts/
docs/*
!docs/voice-guide.md
!docs/voice-guide-baseline.md
!docs/voice-guide-baseline-extra.md
scripts/
# Local Supabase migrations / config — only ignore the root-level dir,
# not src/lib/supabase which contains application code.
/supabase/
tsconfig.tsbuildinfo
# Local one-off scratch: dated telegram-send / verify / debug scripts and
# temp publish staging. Convention: leading underscore = throwaway, never committed.
# (Override a specific file with e.g. `!/_keepme.mjs` if ever needed.)
/_*
